# Génère une clé JWT forte avec :
#   php -r "echo bin2hex(random_bytes(32));"

# Base de données
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=loom

# Application (development | production)
APP_NAME=Loom
APP_ENV=development

# Sécurité JWT
JWT_SECRET=remplace_par_une_cle_aleatoire_longue
JWT_ALGORITHM=HS256
JWT_EXPIRATION=86400
